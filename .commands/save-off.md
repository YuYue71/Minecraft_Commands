# `/save-off`（關閉自動儲存指令）

* > 用於關閉世界的自動儲存機制
* > 停用後世界變更不會自動寫入磁碟
* > 通常與 `/save-on` 搭配使用

## 語法結構 (Syntax)

```commands id="m3q8xn"
/save-off
```

---

## 跨元素語法關聯表 (Links Matrix)

| 關聯參數欄位 | 參引語法元件名稱 |
| ------ | -------- |
| [`/save-on`](save-on.md) | 開啟自動儲存指令 |