# <元素夾名稱> (<英文名稱>)

> **分類:** `資料包元素` | **檔案格式:** `<json | mcfunction>` | **命名空間支援:** `true` | **熱重載:** `/reload`

---

## 目錄

* [目錄路徑](#)
* [核心結構](#)
* [節點說明](#)
    * [節點A](#)
    * [節點B](#)
* [外部連結](#)

---

## 目錄路徑 (Directory & Path)

> 說明該元素檔案在資料包內部的標準存放位置.

```file
data/<namespace>/<元素夾名稱>/[子資料夾]/<檔案名稱>.<附檔名>
```

* `<namespace>` = 命名空間 (如 `minecraft`, `example`)
* `[子資料夾/]` = 開發者自訂的分類目錄 (選填)
* `<檔案名稱>` = 決定遊戲內調用該元素的 ID 名稱
* 遊戲內調用格式通常為: `<namespace>:[子資料夾/]<檔案名稱>`

---

## 核心結構 (Core Structure)

> 說明該檔案的核心骨架結構 (以 JSON 格式或 mcfunction 為主).

```json
{
    "節點A": "value",
    "節點B": {
        "子節點B1": 123,
        "子節點B2": true
    },
    "節點C": [
        "..."
    ]
}

```

* 根目錄結構中, 哪些為必填節點, 哪些為選填節點.

| 鍵值 / 節點 (Key) | 類型 | 必填 | 說明 |
| --- | --- | --- | --- |
| `"節點A"` | `string` | 是 | 簡述作用 |
| `"節點B"` | `object` | 否 | 簡述作用 |
| `"節點C"` | `array` | 否 | 簡述作用 |

---

## 節點說明 (Node Explanations)

### `節點A`

> 說明此節點的具體功能, 合法輸入值與應用場景.

| 值 / 屬性 | 說明 |
| --- | --- |
| `值1` | 簡述 (預設值) |
| `值2` | 簡述 |

---

### `節點B (Object)`

> 若節點為物件或陣列, 在此向下展開其內部的子結構.

| 子節點 (Sub-key) | 類型 | 必填 | 說明 |
| --- | --- | --- | --- |
| `"子節點B1"` | `int` | 是 | 簡述作用 |
| `"子節點B2"` | `boolean` | 否 | 簡述作用 (`false`) |

---

## 外部連結 (References)

* [Minecraft Wiki - <元素名稱>](https://www.google.com/search?q=%23)
* [Misode Data Pack Generators - <元素名稱>](https://misode.github.io/)
* (生成時請依據下方規範第 6 點, 自動插入引用的全域系統連結)

---

# 以下內容為生成規範(生成時請忽略, 無須寫入檔案中)

1. [絕對約束] 標點符號與格式 (Absolute Constraints):

* 整份文件嚴禁使用全形標點符號 (如: ， 。 ： ； （ ）). 所有標點符號一律強制替換為半形 (如: , . : ; ( )).
* 輸出純 Markdown 格式, 嚴禁附加任何提示語, 說明文字或對話. 所有佔位符必須完全替換.

2. 標頭與目錄規則 (Header & TOC Rules):

* 標頭的格式類型必須正確填入 (`json` 或 `mcfunction`).
* 目錄 (TOC) 的連結文字必須使用中文, 連結目的地統一填入 `#`, 不填寫實際錨點 ID.
* 所有 H2/H3 標題後方嚴禁附加 `{#anchor-id}` 語法.

3. 路徑與結構規則 (Path & Structure Rules):

* 必須清楚定義該元素在資料包中的正確存放路徑, 並示範遊戲內的呼叫 ID 寫法.
* 核心結構 (Core Structure) 必須提供一個完整且合法的 JSON (或指令腳本) 骨架範例, 包含最常用的節點.
* 表格標頭必須嚴格使用 `| 鍵值 / 節點 (Key) | 類型 | 必填 | 說明 |`. 「必填」欄位僅能填入 `是` 或 `否`.

4. 巢狀節點與枚舉規則 (Nested Nodes & Enum Rules):

* 若 JSON 節點為深層物件 (Object) 或陣列 (Array), 必須在 `## 節點說明` 下方使用獨立的 H3 標題展開其子節點表格, 禁止將所有層級塞入同一個表格中.
* 枚舉資料 (Enum Data) 或特定鍵值必須查找完整並全部列出, 絕對禁止使用「... 等」或「其餘略」進行省略.
* 若某節點具備預設值, 必須在說明欄位內明確標註 `(預設值)`.

5. 長文件分段規則 (Pagination Rules):

* 若預估 JSON 節點條目過多 (如戰利品表, 條件判斷器等), 導致單次 Token 輸出可能超載, 必須先輸出主體骨架與表頭, 並使用 `` 標註, 待確認後再進行後續分段填補.

6. 全域系統自動映射與去冗餘規則 (Cross-Reference Auto-Linking & No-Redundancy):

* 只要該資料包元素的節點中, 有使用或提及下列任何全域系統機制, 必須強制將對應的 Markdown 連結直接複製並加入到 `## 外部連結 (References)` 區塊中.
* [絕對約束]: 如果生成時有任何引用到下方清單的內容, 絕對不用在該檔案中再生成一次詳細內容或展開解釋, 僅需在節點說明簡述用途, 並直接用超連結指向 Github 連結即可.
* 絕對映射清單 (請完全使用此格式貼上):
* `[目標選擇器 (Target Selectors)](https://github.com/YuYue71/Minecraft_Commands/blob/main/.syntax_components/TargetSelectors.md)`
* `[座標系統 (Coordinate Systems)](https://github.com/YuYue71/Minecraft_Commands/blob/main/.syntax_components/coordinates.md)`
* `[JSON 文本元件 (Raw JSON Text Components)](https://github.com/YuYue71/Minecraft_Commands/blob/main/.syntax_components/json_text.md)`
* `[時間與時間單位 (Time & Duration Formats)](https://github.com/YuYue71/Minecraft_Commands/blob/main/.syntax_components/TimeFormats.md)`
* `[資源位置與命名空間規範 (Resource Locations)](https://github.com/YuYue71/Minecraft_Commands/blob/main/.syntax_components/ResourceLocations.md)`
* `[方塊狀態與標籤 (Block States & Tags)](https://github.com/YuYue71/Minecraft_Commands/blob/main/.syntax_components/BlockStates_or_Tags.md)`
* `[粒子特殊參數 (Particle Extra Arguments)](https://github.com/YuYue71/Minecraft_Commands/blob/main/.syntax_components/ParticleExtraArguments.md)`
* `[實體 NBT 矩陣 (Entities)](https://github.com/YuYue71/Minecraft_Commands/blob/main/.NBT/list.md)`
* `[方塊實體 NBT 矩陣 (Block Entities)](https://github.com/YuYue71/Minecraft_Commands/blob/main/.NBT/list.md)`
* `[物品 NBT 矩陣 (Items)](https://github.com/YuYue71/Minecraft_Commands/blob/main/.NBT/list.md)`



7. 無外部組件映射之窮舉處理原則 (Inline Exhaustive List Fallback):
* 觸發條件: 當節點包含需要列舉的特定原版資料清單 (如 Advancements 的 `trigger` 類型, Loot Tables 的 `type` 類型等), 且該項目不在第 6 項的外部連結清單中時.
* 處理動作: 絕對不可要求讀者自行去 Wiki 查詢. 必須直接在該節點說明的下方, 新增一個獨立區塊 (例如 `### 原版 [觸發器/類型] 清單`).
* 排版要求: 必須將所有原版資料使用高密度的 Markdown 矩陣表格進行歸納排版, 確保清單完整且無冗餘贅字.


8. 管道符號跳脫原則 (Pipe Character Escaping):
* 觸發條件: 當需要在 Markdown 表格的儲存格內部顯示垂直線 `|` 時 (例如表示二擇一的類型 `string\|object`).
* 處理動作: 必須強制使用反斜線進行跳脫, 寫成 `\|`, 絕對不可直接輸出純 `|`, 以防止表格渲染結構損毀.


9. 生成器工具映射原則 (Datapack Generator Rules):
* 處理動作: 資料包內容結構極為複雜, 在 `## 外部連結` 區塊中, 必須強制加入 [Misode Data Pack Generators](https://misode.github.io/) 該對應元素的具體生成器連結, 輔助讀者視覺化生成 JSON 結構.